<%= form_with(model: post, local: true) do |f| %>

  <div class="form-group">
    <%= f.label :image, "Your image", class: "form-label" %>
    <div class="mb-3">
      <%= f.file_field :image, class: "form-control" %>
    </div>
  </div>

  <%= f.submit class: "btn btn-primary" %>
<% end %>
