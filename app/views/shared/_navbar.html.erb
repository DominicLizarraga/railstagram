<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <%= link_to Railstagram, root_path, class: "navbar-brand mb-3"  %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <%= link_to "Explore", root_path, class: "nav-link active"  %>

      </ul>

      <ul class="navbar-nav ms-auto">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Edit", edit_user_registration_path, class: "nav-link "  %>
          </li>
          <li class="nav-item">
            <%= link_to "Upload", root_path, class: "nav-link "  %>
          </li>
          <li class="nav-item">
            <%= link_to user_path(current_user.username), class: "nav-link active" do  %>
              <%= image_tag avatar_url_for(current_user, size: 24), class: "rounded-circle me-1" %>
              <%= current_user.username %>
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "nav-link "  %>
          </li>

        <% else %>
          <li class="nav-item">
            <%= link_to "Log In", new_user_session_path, class: "nav-link active"  %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign Up", new_user_registration_path, class: "nav-link active"  %>
          </li>
        <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>
